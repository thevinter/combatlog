services:
  wcl-uploader:
    build:
      context: .
      dockerfile: web/Dockerfile
    container_name: wcl-uploader
    restart: unless-stopped
    expose:
      - "5050"
    networks:
      - npm_default

networks:
  npm_default:
    external: true
